"""Concrete connector adapters that implement the protocols in core/ports.

Each module exposes one class implementing exactly one Protocol so the DI
container can wire them in app/core/di/container.py.
"""
