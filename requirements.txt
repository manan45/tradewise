# TraderWise — production runtime
# Pinned where stability matters; floating where minor versions are safe.
# See LOCAL_DEV.md for what runs natively on Apple Silicon.

# --- Web / API ---
fastapi==0.115.3
uvicorn[standard]==0.32.0
pydantic==2.9.2
python-dotenv==1.0.1
httpx==0.27.2

# --- Storage / messaging ---
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
asyncpg==0.30.0
alembic==1.13.3
redis==5.2.0
qdrant-client==1.12.1
aio-pika==9.5.0          # RabbitMQ async client (chosen bus per IMPLEMENTATION_PLAN §3.10 Op4)

# --- Data / numerics ---
numpy==1.26.4
pandas==2.2.3
polars==1.13.1            # preferred for hot feature pipeline (LOCAL_DEV §2.2)
pyarrow==17.0.0
scipy==1.14.1
scikit-learn==1.5.2
ta==0.11.0                # technical indicators

# --- Models ---
lightgbm==4.5.0
mlfinlab==2.0.0           # triple-barrier, purged CV, deflated Sharpe
sentence-transformers==3.2.1   # BGE embeddings
transformers==4.46.0           # FinBERT inference

# --- Backtesting ---
vectorbt==0.26.2
exchange-calendars==4.5.6
numba==0.60.0

# --- Brokers / market data ---
alpaca-py==0.34.0
ib-insync==0.9.86         # IBKR for futures (planned; light footprint)
yfinance==0.2.48          # fallback / smoke ingest only

# --- LLM orchestrator ---
anthropic==0.39.0

# --- HITL ---
python-telegram-bot==21.7
twilio==9.3.7

# --- Dashboard ---
streamlit==1.40.1
plotly==5.24.1

# --- Observability ---
prometheus-client==0.21.0
structlog==24.4.0
